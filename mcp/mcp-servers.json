{
  "mcpServers": {
    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp@latest"],
      "description": "Library documentation lookup — resolves to latest docs instead of stale training data"
    },
    "memory": {
      "command": "npx",
      "args": ["-y", "@anthropic/claude-memory-mcp"],
      "description": "Persistent memory across sessions — remembers project decisions, preferences, and context"
    },
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@anthropic/sequential-thinking-mcp"],
      "description": "Step-by-step reasoning for complex problems — system design, debugging, architecture decisions"
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@anthropic/github-mcp"],
      "env": {
        "GITHUB_TOKEN": "<YOUR_GITHUB_TOKEN>"
      },
      "description": "GitHub integration — PRs, issues, repos, code search (requires personal access token)"
    }
  }
}
